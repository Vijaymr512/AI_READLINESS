# ST1 - AI Readiness Assessment Tool (Frontend)

## Setup

1. Install dependencies:
   - `npm install`
2. Run app:
   - `npm run dev`

## API base URL

Set in `src/services/apiClient.js` (default: `http://localhost:8000`).
